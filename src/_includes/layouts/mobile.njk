<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{name}}</title>
</head>
<style>
    *{margin:0;padding:0;}
    body{font-family: sans-serif;}
    h5{text-align: center;}
    img{max-width: 100%; max-height: 600px;}
    ol{list-style-position: inside;}
</style>
<body>

    <h5>{{name}}</h5>
    
    {% if img %}
        <picture>
            {% if webp %}
                <source srcset="/img/webp/{{img|replace('.jpg', '.webp')}}" type="image/webp">
            {% endif %}
            <img src="/img/{{img}}" alt="{{name}}">
        </picture>
    {% endif %}

    <ol>
        {%- for etape in etapes -%}
            <li>{{etape}}</li>
        {%- endfor -%}
    </ol>

    {% if not img %}
        <p>{{content|safe}}</p>
    {% endif %}


</body>
</html>
